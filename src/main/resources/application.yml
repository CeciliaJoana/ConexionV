
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/CONEXIONVOLUNTARIA?useSSL=false&allowPublicKeyRetrieval=true
    username: root
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    show-sql: false
  
server:
  port: 8080
